server {
    listen 80;
    server_name api.darshi.app;

    # Increase timeouts for AI processing
    proxy_read_timeout 300s;
    proxy_connect_timeout 75s;
    
    # Increase max body size for image uploads
    client_max_body_size 50M;

    location / {
        proxy_pass http://darshi-backend:8080;
        proxy_http_version 1.1;
        
        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support (if needed)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Disable buffering for real-time responses
        proxy_buffering off;
    }
}

server {
    listen 80;
    server_name darshi.app www.darshi.app;

    location / {
        proxy_pass http://darshi-frontend:80;
        proxy_http_version 1.1;
        
        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support for HMR
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# Redirect HTTP to HTTPS (uncomment when SSL is configured)
# server {
#     listen 80;
#     server_name darshi.app www.darshi.app api.darshi.app;
#     return 301 https://$server_name$request_uri;
# }
